import * as cdk from '@aws-cdk/core';
import { ChatStack } from '../lib/chat-stack';

test('Nothing', () => {
    const app = new cdk.App();
    // WHEN
    // const stack = new ChatStack(app, 'MyTestStack');
    // THEN
});
